import readFile from '../utils/readFile';


const inputFile = '../d5/input.txt';
const getPlace = (data: string) => { };
const mapNexPlace = (source: number, target: number) => { };

const main = async () => {
    const fileContent: string = await readFile(inputFile);

}
